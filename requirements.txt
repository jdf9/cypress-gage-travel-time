# Colab already ships everything except dataretrieval; the notebook's first cell installs it if missing.
dataretrieval>=1.3
pandas>=2.0
numpy>=1.24
scipy>=1.10
geopandas>=0.14
matplotlib>=3.7
# only needed to run / re-execute the notebook locally
jupyter
